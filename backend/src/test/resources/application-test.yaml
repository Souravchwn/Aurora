spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

news:
  apikeys:
    newsapi: "test-key"
    gnews: "test-key"

summary:
  ai:
    enabled: false

logging:
  level:
    com.sourav.aurora: DEBUG